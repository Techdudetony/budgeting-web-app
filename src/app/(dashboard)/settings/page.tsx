import PageIntro from "@/components/ui/PageIntro";

/* Application settings page. */
export default function SettingsPage() {
  return (
    <div>
      <PageIntro
        title="Settings"
        description="Manage your preferences, account settings, and application behavior."
      />
    </div>
  );
}
